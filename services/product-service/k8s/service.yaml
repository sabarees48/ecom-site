apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: eks-demo
  labels:
    app: product-service
spec:
  type: ClusterIP
  selector:
    app: product-service
  ports:
    - name: http
      port: 80
      targetPort: 3002
      protocol: TCP
